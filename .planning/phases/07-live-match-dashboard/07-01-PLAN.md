# Plan: 07-01 - React + Vite Project Initialization & Core Pages

**Wave:** 1
**Depends on:** Nothing
**Files modified:** None (New Directory: frontend/)
**Autonomous:** true

## Description
This plan initializes the React + Vite project and builds the foundational routing and layout for the Arenex Dashboard.

## Tasks

<task>
<action>
Initialize Vite project:
`npx -y create-vite@latest frontend --template react`
Install dependencies:
`cd frontend && npm install react-router-dom chess.js react-chessboard`
</action>
<acceptance_criteria>
- `frontend/package.json` exists with dependencies.
- `frontend/index.html` exists.
</acceptance_criteria>
</task>

<task read_first="app/core/redis.py">
<action>
Create `frontend/.env`:
`VITE_API_URL=http://localhost:8000`
`VITE_WS_URL=ws://localhost:8000`
</action>
<acceptance_criteria>
- `frontend/.env` contains the correct URLs.
</acceptance_criteria>
</task>

<task>
<action>
Create global styles in `frontend/src/index.css`:
- Dark background: `#1a1a2e`
- Accent color: `#BB86FC`
- Card background: `#16213e`
- Clean reset and Inter/System font family.
</action>
<acceptance_criteria>
- `index.css` reflects the design spec.
- Dark theme is applied to body.
</acceptance_criteria>
</task>

<task>
<action>
Set up routing in `frontend/src/App.jsx`:
- `/` -> Home
- `/match/:id` -> Spectator
- `/leaderboard` -> Leaderboard
- `/register` -> Register Agent
- `/start-match` -> Start Match
</action>
<acceptance_criteria>
- `App.jsx` uses `BrowserRouter`.
- All routes are defined and map to components.
</acceptance_criteria>
</task>

<task read_first="app/api/agents.py">
<action>
Implement `Home.jsx` and `Leaderboard.jsx`:
- Fetch matches and agents from the API.
- Display in styled cards/tables.
</action>
<acceptance_criteria>
- `Home.jsx` displays list of live matches.
- `Leaderboard.jsx` displays agents sorted by ELO.
</acceptance_criteria>
</task>

<task read_first="app/api/ws.py">
<action>
Implement `Spectator.jsx`:
- Use native `new WebSocket()` to connect to the match stream.
- Handle `catchup` and `move` messages.
- Render `Chessboard` for Chess game types.
- Render custom CSS grid for Tic-Tac-Toe.
</action>
<acceptance_criteria>
- Real-time move updates work via WebSocket.
- Both game types are supported in the UI.
</acceptance_criteria>
</task>

## Verification

### must_haves
- [ ] React app initializes and runs via `npm run dev`.
- [ ] Navigation between all 5 pages is functional.
- [ ] Leaderboard shows accurate data from the backend.
- [ ] Spectator view connects to WebSocket and renders the board.

### Automated Tests
- I will run `npm run build` to ensure no linting or build errors.
