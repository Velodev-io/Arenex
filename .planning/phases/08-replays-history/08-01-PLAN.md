# Plan: 08-01 - Interactive Replay Controls

**Wave:** 1
**Depends on:** Nothing
**Files modified:** frontend/src/pages/Spectator.jsx
**Autonomous:** true

## Description
This plan implements interactive replay functionality in the Spectator view, allowing users to scrub through moves, auto-play matches, and jump to specific timestamps.

## Tasks

<task read_first="frontend/src/pages/Spectator.jsx">
<action>
Add playback state to `Spectator.jsx`:
- `viewIndex`: Number (tracks the current move being displayed).
- `isAutoPlaying`: Boolean.
- `playbackSpeed`: Number.
</action>
<acceptance_criteria>
- State variables defined in the component.
</acceptance_criteria>
</task>

<task read_first="frontend/src/pages/Spectator.jsx">
<action>
Update WebSocket and Fetch effects:
- On match fetch or WebSocket `catchup`/`move`, update `history`.
- If user is in "Live" mode (viewIndex at end), update the board view.
- If user is in "Review" mode (viewIndex < history.length), do NOT update the board view automatically.
</action>
<acceptance_criteria>
- New moves are added to history but don't disrupt the board view if the user is scrubbing back.
</acceptance_criteria>
</task>

<task>
<action>
Implement `ReplayControls` component (inline or separate):
- Buttons: First, Prev, Play/Pause, Next, Last.
- Logic to increment/decrement `viewIndex`.
- Logic for `setInterval` when `isAutoPlaying` is true.
</action>
<acceptance_criteria>
- Controls appear below the board.
- Buttons correctly update the board state.
</acceptance_criteria>
</task>

<task>
<action>
Enhance Move List sidebar:
- Make each move clickable.
- Highlight the active move based on `viewIndex`.
</action>
<acceptance_criteria>
- Clicking a move in the sidebar jumps the board to that move.
- Active move has a distinct background (e.g., `#2a2a4a`).
</acceptance_criteria>
</task>

## Verification

### must_haves
- [ ] User can step back to move 0 and forward to the current move.
- [ ] Native board rendering (Chess/TTT) updates correctly based on `viewIndex`.
- [ ] Auto-play advances the game automatically.
- [ ] Live matches don't "jump" to the end if the user is reviewing previous moves.

### Automated Tests
- I will verify the build passes after the changes.
