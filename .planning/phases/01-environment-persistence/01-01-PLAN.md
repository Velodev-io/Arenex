# Plan: 01-01 - Infrastructure & Initial Schema

**Wave:** 1
**Depends on:** Nothing
**Files modified:** requirements.txt, app/database.py, app/models/base.py, app/models/user.py, app/models/agent.py, app/models/match.py
**Autonomous:** true

## Description
This plan sets up the foundational FastAPI application structure and the PostgreSQL persistence layer. We will define the core models and initialize the migration system.

## Tasks

<task read_first="requirements.txt">
<action>
Add the following dependencies to `requirements.txt`:
- `fastapi`
- `uvicorn[standard]`
- `sqlalchemy[asyncio]`
- `asyncpg`
- `alembic`
- `pydantic-settings`
- `redis`
</action>
<acceptance_criteria>
- `requirements.txt` contains all specified packages.
</acceptance_criteria>
</task>

<task>
<action>
Create the FastAPI directory structure:
- `app/`
- `app/core/`
- `app/models/`
- `app/schemas/`
- `app/api/`
</action>
<acceptance_criteria>
- All directories exist in the project root.
</acceptance_criteria>
</task>

<task read_first=".planning/research/STACK.md">
<action>
Create `app/database.py` with the following:
- Async engine initialization using `create_async_engine`.
- `AsyncSessionLocal` sessionmaker.
- `Base` declarative base class.
- `get_db` dependency for FastAPI routes.
</action>
<acceptance_criteria>
- `app/database.py` defines `engine`, `AsyncSessionLocal`, and `get_db`.
</acceptance_criteria>
</task>

<task read_first=".planning/PROJECT.md">
<action>
Define models in `app/models/`:
- `User`: id, email, hashed_password, is_active.
- `Agent`: id, name, owner_id (FK to User), endpoint_url, game_type, elo.
- `Match`: id, agent_white_id (FK), agent_black_id (FK), status, history (JSONB).
</action>
<acceptance_criteria>
- `user.py`, `agent.py`, and `match.py` exist with correct fields.
</acceptance_criteria>
</task>

<task>
<action>
Initialize Alembic for async migrations:
`alembic init -t async alembic`
Update `alembic.ini` and `alembic/env.py` to point to the `Base` in `app/models/`.
</action>
<acceptance_criteria>
- `alembic/` directory exists.
- `alembic/env.py` imports `Base.metadata`.
</acceptance_criteria>
</task>

<task>
<action>
**[BLOCKING] Initial Migration**
Generate and run the initial migration:
`alembic revision --autogenerate -m "initial migration"`
`alembic upgrade head`
</action>
<acceptance_criteria>
- Database tables (users, agents, matches) are created in PostgreSQL.
</acceptance_criteria>
</task>

<task read_first="app/database.py">
<action>
Add Redis connection logic to `app/core/redis.py`:
- Initialize `redis.asyncio.Redis` client.
- Create a `get_redis` dependency.
</action>
<acceptance_criteria>
- `app/core/redis.py` exists and can connect to a local Redis instance.
</acceptance_criteria>
</task>

## Verification

### must_haves
- [ ] Database connection is successful.
- [ ] Alembic can generate and apply migrations.
- [ ] Pydantic settings load from `.env`.

### Automated Tests
- I will run a script to verify DB connectivity: `python -c "import app.database; print('Success')"`

---
*Created: 2026-04-11*
